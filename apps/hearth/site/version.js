// Stamped at deploy time by scripts/stamp-build.mjs (see the deploy workflow).
// The placeholders stay as they are when running locally.
export const BUILD = { id: '__BUILD_ID__', date: '__BUILD_DATE__' }
